{
	"name": "frame-forge-worker",
	"private": true,
	"type": "module",
	"version": "0.0.0",
	"description": "Standalone reconstruction worker (Agent SDK + headless render + GLB export). Isolated from the Next app's deps.",
	"scripts": {
		"start": "node worker.mjs",
		"render-test": "node render-test.mjs"
	},
	"dependencies": {
		"@anthropic-ai/claude-agent-sdk": "^0.3.218",
		"esbuild": "^0.28.1",
		"playwright": "^1.61.1",
		"three": "^0.185.1"
	}
}
